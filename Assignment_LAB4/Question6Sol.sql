select category.cat_id,category.cat_name,product.pro_name,supplier_pricing.supp_price from supplier_pricing join ( product join category on product.cat_id = category.cat_id) on supplier_pricing.pro_id = product.pro_id group by category.cat_id having min(supp_price);
